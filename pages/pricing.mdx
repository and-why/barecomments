import DocsLayout from '@/components/DocsLayout';

export default (props) => <DocsLayout {...props} />;

# Pricing

Bare Comments is still in alpha. It's free to sign up and leave feedback
and $3.50/month for unlimited sites and feedback.

Reach out to contact@andysmith.is if you have any questions.
